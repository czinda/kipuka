# Real in-process HTTP/CMS/CoAP regression tests run in unit-tests across the
# workspace. No placeholder job reports interoperability or FIPS certification.
# Dogtag/FreeIPA/Beaker provisioning scripts require dedicated disposable hosts
# and do not yet test CI_COMMIT_SHA reliably. See docs/ci-gitlab.md.
{}
